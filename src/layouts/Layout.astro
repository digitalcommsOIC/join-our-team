---
interface Props {
	title: string;
	icon: string;
	children: any;
  }
  
const { title, icon, children } = Astro.props;
  
import { ViewTransitions } from 'astro:transitions';
import Footer from '../components/footer.astro';
import '../styles/global.css';
import PostHog from '../components/posthog.astro';

import { SEO } from "astro-seo";

// import PocketBase from 'pocketbase';

// const url = 'https://oic-careers.pockethost.io/'
// const client = new PocketBase(url)

// const records = await client.collection('posts').getFullList({
//     sort: '-created',
// });
---

<!doctype html>
<html lang="en" class="scroll-smooth scrollbar-none bg-[#1e1e1e]">
	<head>

		<SEO 
			title="Orkney Islands Council Jobs"
			description="Join Orkney Islands Council and become part of a dynamic community that values innovation, sustainability, and inclusivity. Discover rewarding career opportunities with guidance on the application process, insights into the council's core values, and information on the unique lifestyle that comes with living in Orkney. Make a meaningful impact whilst enjoying the stunning landscapes and tight-knit island community."
			openGraph={{
				basic: {
					title: "Orkney Islands Council Jobs",
					type: "website",
					image: "https://joinus.orkneyislands.uk//images/Aerial-view-over-Churchill-Barriers-Orkney-Orkneycom.webp"
				}
			}}
		/>

		<meta charset="UTF-8" />
		<meta name="description" content="Astro description" />
		<meta name="viewport" content="width=device-width" />
		<link rel="icon" type="image/svg+xml" href={icon} />
		<meta name="generator" content={Astro.generator} />
		<PostHog />
		<link rel="sitemap" href="/sitemap-index.xml" />
		<title>{title}</title>
		<ViewTransitions />
	</head>
	<body class="bg-white">
		<slot />
	</body>
	<Footer />
</html>

<style is:global>
	@font-face {
		font-family: 'Orkney';
		src: url('/fonts/orkney.woff') format('woff');
	}
	h1 {
		font-family: 'Orkney', sans-serif !important;
	}
	.orkney {
		font-family: 'Orkney', sans-serif !important;
	}
</style>
